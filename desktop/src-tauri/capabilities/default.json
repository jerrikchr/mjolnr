{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for mjolnr-desktop bridge",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
